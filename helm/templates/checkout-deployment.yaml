apiVersion: apps/v1
kind: Deployment
metadata:
  name: checkout
  labels:
    app: checkout
spec:
  replicas: 1
  selector:
    matchLabels:
      app: checkout
  template:
    metadata:
      labels:
        app: checkout
    spec:
      containers:
        - name: checkout
          image: "{{ .Values.checkout.image }}:{{ .Values.checkout.tag }}"
          ports:
            - containerPort: 8080
          env:
            - name: PORT
              value: "8080"
            - name: RETAIL_CHECKOUT_PERSISTENCE_PROVIDER
              value: "in-memory"
            - name: RETAIL_CHECKOUT_ENDPOINTS_ORDERS
              value: "http://orders:8080"
          resources:
            requests:
              cpu: {{ .Values.checkout.resources.requests.cpu }}
              memory: {{ .Values.checkout.resources.requests.memory }}
            limits:
              cpu: {{ .Values.checkout.resources.limits.cpu }}
              memory: {{ .Values.checkout.resources.limits.memory }}
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
