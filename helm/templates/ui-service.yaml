apiVersion: v1
kind: Service
metadata:
  name: ui
  namespace: {{ .Values.namespace }}
  labels:
    app: ui
spec:
  selector:
    app: ui
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
