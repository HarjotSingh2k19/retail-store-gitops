apiVersion: v1
kind: Service
metadata:
  name: checkout
  namespace: {{ .Values.namespace }}
  labels:
    app: checkout
spec:
  selector:
    app: checkout
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
